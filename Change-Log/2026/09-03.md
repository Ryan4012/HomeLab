# September 3, 2026

## Worked On
- Rate limiting through SQM for my Guest and IoT Wi-Fi.
- Rate limiting on my ICMP server firewall rule.
- Added Node exporter on my Debian VM.
- Configure notifications for my Debian VM system info.

## What I Did
- On my router web dashboard I went to SQM QoS and clicked Add. I then selected the proper interface, set the limit in kbits/sec, and selected enable. This was followed for both Guest and IoT Wi-Fi.
- I then connected my phone to each Wi-Fi network and ran internet tests to ensure the limits were properly implemented and configured for the interfaces.
- Also on my router dashboard I selected my firewall traffic rules, located my ICMP rule, selected Edit, moved to the Advanced tab, and set a limit under Limit matching packets.
- For my Debian VM I installed the Node exporter, configured my prometheus.yml file, and tested the connection of the node exporter system info to prometheus.
- Started to setup grafana alert rules for both the VM and pve node.

## Issues / Findings
- My router needed a proper restart after configuring the rate limiting rules. The router web dashboard was running a little slow and may have needed a reboot since adding new rules to SQM.
- On grafana I had to adjust some of the queries for the alert rules since the system was serving slightly different names than expected for CPU, RAM, Disk, etc. stats.
- Some arithmetic had to be added to the queries as well to ensure percentage values were being checked for the alert rules, instead of some given decimal value. (0.1345 vs 0.001345)

## Next
- Plan and configure my grafana dashboard for my newly added VM system stats.
- Finish Notification rules for my debian vm and proxmomx node.
- Add Cloudflare DNS to my pi hole service and select DNSSEC.
